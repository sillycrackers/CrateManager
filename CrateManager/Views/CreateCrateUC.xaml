<UserControl x:Class="CrateManager.Views.CreateCrateUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrateManager.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.RowSpan="3">
            <TextBlock Text="Crate Name:" Style="{StaticResource TextBlockOnDark}"
                       Margin="10"/>
            <TextBox Text="{Binding Name, Mode=OneWayToSource}" Margin="10,0,10,10" />
            <TextBlock Text="Category:" Style="{StaticResource TextBlockOnDark}"
                       Margin="10"/>
            <ComboBox Margin="10,0,10,10" ItemsSource="{Binding Categories}"/>
            
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="3" Grid.RowSpan="3">
            <TextBlock  Text="Add New Category:" Style="{StaticResource TextBlockOnDark}"
                       Margin="10" FontSize="15"/>
            <TextBox x:Name="AddCategoryTextBox" Text="{Binding Name, Mode=OneWayToSource}" Margin="10,0,10,10" />
            <Button Content="Add" Margin="10,5,10,0" Command="{Binding AddCategoryCommand}" 
                    CommandParameter="{Binding ElementName=AddCategoryTextBox, Path=Text}"/>
        </StackPanel>
        
        
    </Grid>
</UserControl>
